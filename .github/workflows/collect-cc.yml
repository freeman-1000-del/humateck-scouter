name: Weekly CC Collect (DISABLED)

# YouTube API 절약을 위해 CC 자동 수집 중지.
# 필요 시에만 workflow_dispatch로 수동 실행.
on:
  workflow_dispatch:

jobs:
  collect-cc:
    runs-on: ubuntu-latest
    steps:
      - name: Skip — CC collect disabled
        run: |
          echo "CC collect is disabled. Use Japan long/shorts collector (fetch.mjs) instead."
          exit 0
